name: 'Check directory permission'
description: 'Action that allows to check PR if all edited files are permitted to edit by CODEOWNERS config'
#inputs:
#  who-to-greet:  # id of input
#    description: 'Who to greet'
#    required: true
#    default: 'World'
#outputs:
#  time: # id of output
#    description: 'The time we greeted you'
runs:
  using: 'node16'
  main: 'index.js'